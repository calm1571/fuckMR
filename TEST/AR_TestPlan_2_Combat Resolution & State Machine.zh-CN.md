## 测试模块

### 2. 战斗结算与状态机
验证命中、伤害、护盾、死亡、胜负等核心战斗规则是否正确。

**包含内容**
- 命中检测
- 命中 / 未命中结算
- 单次命中伤害计算
- 多次命中累计伤害
- 护盾优先级
- Spectator 权威支援加血结算
- 障碍墙挡弹与墙体 HP 结算
- HP 下限钳制
- 致命命中的结果切换
- 死亡后的状态锁定
- Host 权威胜负判定
- 新一局对临时战斗状态的重置

---

## 模块：战斗结算与状态机

> 说明：  
> - 本模块主要用于 **EditMode / 逻辑层测试**。  
> - 重点验证当前版本中的命中判定、伤害结算、护盾阻挡、Spectator 支援加血、障碍墙交互、HP 钳制以及结果切换是否正确且可确定复现。  
> - 执行测试前请保持 `Actual Outcome` 和 `Status` 为空。  
> - `Status` 建议使用：`Not Run`、`Pass`、`Fail`、`Blocked`、`N/A`。  
> - 若某项依赖当前版本中尚未确认实现的功能，请先标记为 `Blocked`，并补充实现核查链接或说明。
>
> 量化通过标准：
> - 单次有效命中的伤害值必须与当前构建配置下的 `GetDamage()` 完全一致。
> - 多次有效命中必须严格按 `N * GetDamage()` 累计，直到 HP 归零；HP 不允许低于 `0`。
> - 护盾激活窗口内应 `100%` 阻挡来袭伤害；被挡住的命中对 HP 的改变量必须为 `0`。
> - Spectator 支援加血必须恰好增加 `GetSpectatorHealAmount()`，但 HP 不允许超过 `GetMaxHp()`。
> - 被活跃墙体拦截的 Projectile 对玩家 HP 的改变量必须为 `0`；每次有效打墙命中对墙 HP 的改变量必须恰好为 `GetWallShotDamage()`，且墙 HP 不允许低于 `0`。
> - 致命伤害生效后，同一局内后续命中尝试对 HP 的改变量必须为 `0`。
> - 比赛结果必须在致命命中结算后 `500 ms` 内收敛为唯一胜者，之后不得发生胜者翻转。
> - 新一局重置必须完整清除临时战斗状态：Retry 后 HP 重置、护盾门限重置、Spectator 加血冷却重置、活跃墙数量重置为 `0`。

### 执行总结

|项目|结果|
|---|---|
|执行结果|已完成|
|整体状态|Pass|
|通过率|31 / 31|
|Blocked / N/A|0 / 0|
|备注|核心战斗、Spectator 加血、障碍墙以及 Retry 重置相关用例均已执行并全部通过。|

---

#### 功能：命中结算（只有通过 host 侧几何命中判定的射击才会造成伤害）

|Test|Inputs|Expected Outcome|Actual Outcome|Status|
|---|---|---|---|---|
|HR-01 直接命中目标头部位置|A 发射一发子弹，其路径在命中半径内穿过目标头部位置|目标 HP 恰好减少 `GetDamage()` 一次|直接命中场景下，HP 按配置伤害值准确扣减一次。|Pass|
|HR-02 超出命中半径时判定为未命中|A 发射一发子弹，其最近点距离始终大于命中半径|不会发生 HP 变化，也不会触发结果切换|超出命中半径的射击未造成伤害，也未触发结果切换。|Pass|
|HR-03 超出最大距离的射击|A 发射方向有效，但目标位于 `shot.maxDistance` 之外|不会判定命中，也不会发生 HP 变化|超过最大距离的射击未被判定命中，HP 保持不变。|Pass|
|HR-04 位于射手身后的目标|目标位置位于射击生成点后方|不会判定命中，也不会发生 HP 变化|目标位于射手身后的场景未产生命中结算或 HP 变化。|Pass|
|HR-05 多次有效命中累计正确|在无护盾时连续应用多次有效命中|HP 按 `N * GetDamage()` 累计下降，直到归零；不会额外多扣|多次有效命中按预期精确累计，直到 HP 归零。|Pass|

---

#### 功能：护盾阻挡（护盾激活窗口内会阻挡来袭射击）

|Test|Inputs|Expected Outcome|Actual Outcome|Status|
|---|---|---|---|---|
|SH-01 护盾激活时阻挡命中|目标护盾结束时间仍在未来，且本次射击原本会命中|本次射击被护盾阻挡；HP 保持不变|护盾激活窗口内成功完全阻挡伤害，HP 保持不变。|Pass|
|SH-02 护盾过期后命中恢复生效|同一目标在护盾过期后再次被命中|护盾窗口结束后，伤害恢复正常结算|护盾结束后再次命中会正常造成伤害。|Pass|
|SH-03 护盾激活受冷却限制|在 `shieldCooldownUntil` 到期前再次尝试开盾|第二次激活被拒绝；不会延长护盾时间|冷却期间重复开盾被正确拒绝，护盾持续时间未被延长。|Pass|
|SH-04 护盾持续期间不可重复激活|护盾仍在有效期内时再次触发护盾|重复激活被拒绝；护盾结束时间不会被提前刷新|护盾生效期间无法重复激活，结束时间未被刷新。|Pass|
|SH-05 无护盾时正常受伤|目标当前无激活护盾且受到有效命中|HP 恰好减少 `GetDamage()`|无护盾状态下的有效命中按配置值正常扣血。|Pass|

---

#### 功能：Spectator 支援加血结算（Spectator 支援只能通过 Host 权威结算生效）

|Test|Inputs|Expected Outcome|Actual Outcome|Status|
|---|---|---|---|---|
|HL-01 Heal Host 按配置值加血|Host 当前 HP 低于上限；Spectator 触发一次 `Heal Host`|Host HP 恰好增加 `GetSpectatorHealAmount()`，并受 `GetMaxHp()` 上限限制|Heal Host 会按配置值正确提升 Host HP，并在最大 HP 处封顶。|Pass|
|HL-02 Heal Client 按配置值加血|Client 当前 HP 低于上限；Spectator 触发一次 `Heal Client`|Client HP 恰好增加 `GetSpectatorHealAmount()`，并受 `GetMaxHp()` 上限限制|Heal Client 会按配置值正确提升 Client HP，并在最大 HP 处封顶。|Pass|
|HL-03 加血冷却阻止重复支援|在 spectator-vote 冷却结束前再次触发 Heal|第二次加血请求被忽略或拒绝；HP 不会再次变化|冷却期间的重复加血请求被正确忽略，未产生额外 HP 变化。|Pass|
|HL-04 非法加血目标不会污染战斗状态|Host 收到一个目标角色非法或不受支持的 Heal 请求|不会发生 HP 变化，也不会修改无关战斗状态|非法加血目标被安全忽略，未污染 HP 或其他战斗状态。|Pass|

---

#### 功能：HP 边界与死亡后锁定（HP 不会为负，死亡目标不再继续受伤）

|Test|Inputs|Expected Outcome|Actual Outcome|Status|
|---|---|---|---|---|
|HP-01 过量伤害不会使 HP 变负|目标 HP=10，并受到高于剩余 HP 的致命伤害|HP 变为 0，不会出现负数|过量伤害会将 HP 钳制到 0，不会出现负值。|Pass|
|HP-02 死亡后再次受击被忽略|目标 HP 已为 0，再次收到有效射击|不会继续扣减 HP|死亡后的再次受击未继续扣减 HP。|Pass|
|HP-03 连续受击时 HP 递减精确|重复施加非致命命中，直到接近 0|HP 每次按预期数值递减，不会出现 off-by-one|连续非致命命中的 HP 递减精确，无 off-by-one 问题。|Pass|
|HP-04 致命命中只触发一次结果切换|目标被一次致命命中打到 0|结果切换仅发生一次；后续被忽略的命中不会再次触发|致命命中仅触发一次结果切换，后续忽略命中不会重复触发。|Pass|

---

#### 功能：障碍墙结算（墙体会挡弹、掉血，并在归零时销毁）

|Test|Inputs|Expected Outcome|Actual Outcome|Status|
|---|---|---|---|---|
|WO-01 墙体先于玩家拦截 Projectile|活跃墙体位于射击路径上并挡在 shooter 与 target 之间|玩家 HP 保持不变；本次射击被墙体拦截，不继续命中玩家|活跃墙体成功先于玩家拦截子弹，玩家 HP 保持不变。|Pass|
|WO-02 打墙命中按配置值扣墙血|一发有效 Projectile 命中活跃墙体一次|墙 HP 恰好减少 `GetWallShotDamage()`；若未归零则继续存活|有效打墙命中按配置值准确扣减墙体 HP。|Pass|
|WO-03 墙 HP 归零时只销毁一次|连续打墙或自然衰减使墙 HP 降到 `0`|墙 HP 变为 `0` 且不会为负；销毁逻辑只触发一次|墙体 HP 正确钳制到 0，且销毁逻辑只执行一次。|Pass|
|WO-04 已销毁墙体不再继续挡弹|某面墙在 HP=0 后已被移除|后续射击不再被该墙拦截，战斗结算回到正常命中路径|墙体销毁后不再继续挡弹，后续射击恢复正常战斗结算路径。|Pass|

---

#### 功能：结果切换（由 host 权威地在致命命中后结束比赛并确定唯一胜者）

|Test|Inputs|Expected Outcome|Actual Outcome|Status|
|---|---|---|---|---|
|WR-01 Host 击杀 Client|Client HP 因一次 host 侧有效命中降到 0|Host 进入结果界面并发送 `winnerRole = Host`|Host 击杀路径正确进入结果状态，并发送 `winnerRole = Host`。|Pass|
|WR-02 Client 击杀 Host|Host HP 因一次客户端射击被 host 结算后降到 0|Host 以 `winnerRole = Client` 结束比赛，双方分别显示胜负|Client 获胜路径结算正确，双方胜负显示符合预期。|Pass|
|WR-03 非致命命中不会结束比赛|一次有效命中后目标 HP 仍大于 0|只更新 HP，不切换到 Result 状态|非致命命中仅更新战斗状态，未切换到 Result。|Pass|
|WR-04 远端结果载荷能正确映射本地 WIN / LOSE|接收到 `winnerRole = Host` 或 `Client` 的 `MatchResultPayload`|各端都能正确映射为本地 `WIN` 或 `LOSE`|远端结果载荷在各端都能正确映射为本地胜负结果。|Pass|
|WR-05 新一局重置后战斗状态恢复初始值|在一局结束后触发 rematch reset|HP、护盾计时、射击冷却门限和结果文本都恢复到初始状态|rematch reset 后，战斗状态、计时器和结果文本均恢复初始值。|Pass|

---

#### 功能：回合重置与临时状态清理（Retry 会清空支援状态与临时战斗对象）

|Test|Inputs|Expected Outcome|Actual Outcome|Status|
|---|---|---|---|---|
|RS-01 Retry 清空活跃障碍墙|在有一面或多面活跃墙的情况下结束比赛并触发 Retry|新一局开始时活跃墙数量 = `0`；旧墙不会残留在战斗或可视化中|Retry 正确清空了所有活跃障碍墙，新一局没有残留旧墙。|Pass|
|RS-02 Retry 清空 Spectator 支援冷却|上一局结束前不久 Spectator 刚触发 Heal，然后通过 Retry 开启新一局|新一局中的 Spectator 支援冷却按设计重置；不会错误继承旧局冷却|Retry 正确重置了 Spectator 支援冷却，没有错误继承上一局冷却状态。|Pass|
|RS-03 Retry 清空结果锁与死亡锁|上一局以致命结果结束，再通过 Retry 开启下一局|新一局可以正常接受新的伤害 / 护盾 / 命中结算；不会卡在上一局结果锁中|Retry 正确清除了结果锁，下一局可以正常进入新的命中与护盾结算。|Pass|
|RS-04 Retry 后仍遵循初始 HP / 伤害 / 加血规则|Retry 后再次进行普通命中和可选 Heal|新一局中的伤害、加血上限与 HP 边界仍与新开局一致|Retry 后的伤害、加血上限与 HP 边界规则与新开局保持一致。|Pass|

---
