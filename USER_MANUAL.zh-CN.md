# 用户手册

## 1. 文档目的

本手册面向项目使用者、演示人员和验收人员，说明如何在 PICO 设备上运行和操作本系统，不涉及开发细节。

## 2. 推荐使用配置

完整体验建议在同一物理空间中使用三台 PICO 头显：

- 1 台 Host
- 1 台 Client
- 1 台 Spectator

当前主目标设备：
- PICO 4 Ultra

## 3. 使用前准备

开始前请确认：

- 三台设备连接到同一个局域网
- 三台设备都已经安装应用
- 现场空间足够安全、无遮挡危险物
- 三位参与者处于同一个真实物理空间内
- 透视和相机相关权限已经允许

## 4. 角色说明

### 4.1 Host
Host 为权威端。

主要职责：
- 启动比赛
- 执行其中一个校准步骤
- 执行最终确认
- 负责权威战斗结算

### 4.2 Client
Client 为第二位玩家。

主要职责：
- 输入 Host IP 并连接
- 执行其中一个校准步骤
- 与 Host 对战

### 4.3 Spectator
Spectator 为观战与支援角色。

主要职责：
- 输入 Host IP 并连接
- 执行两个校准步骤
- 可为玩家投票加血
- 可触发本地弹幕
- 可播放本地欢呼音效
- 可放置临时障碍墙

## 5. 启动流程

### 5.1 启动 Host
1. 在 Host 设备上打开应用。
2. 在主菜单中选择 `Start Game`。
3. 选择 `Host`。
4. 进入 Host Lobby 后，查看界面中的 `Local IP`。

### 5.2 启动 Client
1. 在 Client 设备上打开应用。
2. 选择 `Start Game`。
3. 选择 `Client`。
4. 在 Lobby 界面的 `Host IP` 输入框中填入 Host 的 `Local IP`。
5. 点击 `Connect`。

### 5.3 启动 Spectator
1. 在 Spectator 设备上打开应用。
2. 选择 `Start Game`。
3. 选择 `Spectator`。
4. 在 Lobby 界面的 `Host IP` 输入框中填入 Host 的 `Local IP`。
5. 点击 `Connect`。

### 5.4 开始比赛
当 Host、Client、Spectator 都连接成功后，Host 可以点击 `Start Match`。

## 6. 五步串行校准流程

系统使用五步串行校准。每一步只有指定设备可以操作，其他设备不能干涉。

### 第 1 步
- 当前角色：Client
- 任务：Client 调整自己眼中的 Host 视觉位置
- 操作完成后：Client 点击 `Confirm Step`

### 第 2 步
- 当前角色：Host
- 任务：Host 调整自己眼中的 Client 视觉位置
- 操作完成后：Host 点击 `Confirm Step`

### 第 3 步
- 当前角色：Spectator
- 任务：Spectator 调整自己眼中的 Client 视觉位置
- 操作完成后：Spectator 点击 `Confirm Step`

### 第 4 步
- 当前角色：Spectator
- 任务：Spectator 调整自己眼中的 Host 视觉位置
- 操作完成后：Spectator 点击 `Confirm Step`

### 第 5 步
- 当前角色：Host
- 任务：最终确认
- 操作完成后：Host 点击 `Confirm`

只有第 5 步完成后，系统才会进入 `Playing`。

## 7. 校准阶段操作键位

在当前设备被允许执行校准时，可以使用以下操作：

- 右摇杆：在 `XZ` 平面移动远端视觉体
- `A`：上移
- `B`：下移
- 按住 `X`：沿一个方向绕可见头部旋转
- 按住 `Y`：沿相反方向绕可见头部旋转

不是当前步骤负责方的设备不能干预。

## 8. 玩家对战操作

### 8.1 发射子弹
- `Trigger`：发射子弹

### 8.2 护盾
- 护盾按键：激活护盾

玩法说明：
- 命中判定由 Host 权威裁决
- 子弹会被护盾或障碍墙阻挡
- 命中后会扣除 HP

## 9. 结算与重赛

比赛结束后：
- 胜者显示 `WIN`
- 败者显示 `LOSE`
- Spectator 会看到正确的胜者结果

重赛流程：
1. Host 和 Client 都点击 `Retry`
2. 系统重置本局战斗状态
3. 直接进入下一局 `Playing`

## 10. Spectator 功能

### 10.1 投票加血
Spectator 控制面板中可选择：

- `Heal Host`
- `Heal Client`

是否真正加血由 Host 权威决定，并受冷却限制。

### 10.2 本地弹幕
Spectator 可以触发固定词汇弹幕，例如：
- `COOL`
- `GOOD GAME`
- `NICE SHOT`

这些弹幕只会显示在 Spectator 自己的设备上。

### 10.3 本地音效
Spectator 可以触发本地音效：
- `Cheer`
- `Applause`

这些音效只会在 Spectator 自己的设备上播放。

### 10.4 放置障碍墙
Spectator 可以放置一个临时障碍墙。

放置流程：
1. 点击 `Place Wall`
2. 观众前方出现一个预览墙
3. 调整预览墙的位置和朝向
4. 确认放置
5. Host 生成真正的权威障碍墙

障碍墙效果：
- 子弹碰到后会被销毁
- 墙会随时间自动掉血
- 墙被子弹击中也会扣血
- 血量归零后墙会消失

## 11. 放墙预览键位

当 Spectator 进入障碍墙预览模式时：

- 右摇杆：移动预览墙的 `XZ`
- `A`：上移
- `B`：下移
- 按住 `X / Y`：绕 `Y` 轴旋转
- 右扳机：确认放置
- 左扳机：取消放置

## 12. 重要视觉标记

系统中的重要视觉标记包括：
- 远端左右手上方的 `L` / `R`
- 远端头部正面的 `+`
- 玩家头顶血条
- 障碍墙顶部血条
- 障碍墙随血量降低逐渐增加的裂痕

## 13. 常见问题

### 13.1 三台设备连不上
请检查：
- 是否连接到同一个局域网
- Host 是否已经先进入 Host Lobby
- Client 和 Spectator 输入的 `Host IP` 是否与 Host 界面的 `Local IP` 一致
- Lobby 的 `Diag` 文案是否显示了连接进度

### 13.2 校准流程卡住
请检查：
- 当前步骤是否由正确的设备在操作
- 当前步骤负责设备是否点击了 `Confirm Step`
- 三台设备是否都还保持连接

### 13.3 视觉对齐不准确
请检查：
- 是否在正确步骤由正确角色执行对齐
- 是否围绕头部正面的 `+` 做小幅调整
- 是否避免一次性大幅移动和旋转

### 13.4 墙没有出现
请检查：
- Spectator 是否点击了 `Place Wall`
- 是否是确认放置，而不是取消
- 当前障碍墙数量上限或冷却是否阻止了请求

## 14. 安全注意事项

- 请在空旷、安全的物理空间中使用
- 参与者之间保持安全距离
- 避免靠近家具、墙面或障碍物快速移动
- 若透视效果不清晰，请暂停演示并重新确认环境
